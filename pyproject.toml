[project]
name = "sv-terminal-mvp"
version = "0.1.0"
description = "SV Terminal MVP local worker prototype"
requires-python = ">=3.11"

[project.scripts]
sv-terminal-worker = "sv_terminal_worker.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
